"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowUpRight, CheckCircle2, Clock3, PackageCheck, Plus, Search, ShoppingBag, Truck } from "lucide-react";
import { mockOrders, Order } from "@/lib/mock-data";

const statusStyles: Record<Order["status"], string> = {
  PENDING: "bg-amber-50 text-amber-700 border-amber-200", CONFIRMED: "bg-sky-50 text-sky-700 border-sky-200", PROCESSING: "bg-blue-50 text-blue-700 border-blue-200", SHIPPED: "bg-violet-50 text-violet-700 border-violet-200", DELIVERED: "bg-emerald-50 text-emerald-700 border-emerald-200", CANCELLED: "bg-rose-50 text-rose-700 border-rose-200", Returned: "bg-slate-100 text-slate-700 border-slate-200", Pending: "bg-amber-50 text-amber-700 border-amber-200", Processing: "bg-blue-50 text-blue-700 border-blue-200", Delivered: "bg-emerald-50 text-emerald-700 border-emerald-200", Cancelled: "bg-rose-50 text-rose-700 border-rose-200",
};

const statusLabel = (status: Order["status"]) => status.charAt(0) + status.slice(1).toLowerCase();

export default function ManualOrderDashboardPage() {
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState<"ALL" | Order["status"]>("ALL");
  const manualOrders = mockOrders.filter((order) => order.isManual);
  const filteredOrders = manualOrders.filter((order) => {
    const searchable = `${order.orderNumber} ${order.customerName} ${order.customerPhone}`.toLowerCase();
    return searchable.includes(search.toLowerCase()) && (status === "ALL" || order.status === status);
  });
  const pendingOrders = manualOrders.filter((order) => order.status === "PENDING" || order.status === "Pending").length;
  const processingOrders = manualOrders.filter((order) => order.status === "PROCESSING" || order.status === "Processing").length;
  const collectedAmount = manualOrders.filter((order) => order.paymentStatus === "PAID").reduce((total, order) => total + order.totalAmount, 0);

  return (
    <div className="space-y-6">
      <div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-emerald-600">Counter workspace</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-slate-950">Manual orders</h1><p className="mt-1 text-sm text-slate-500">Keep phone and inbox orders moving from entry to delivery.</p></div><Link href="/manual-order-entry/orders/new" className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition-colors hover:bg-emerald-700"><Plus className="size-4" />New order</Link></div>
      <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Manual orders" value={manualOrders.length} icon={ShoppingBag} tone="emerald" /><MetricCard label="Needs confirmation" value={pendingOrders} icon={Clock3} tone="amber" /><MetricCard label="In progress" value={processingOrders} icon={Truck} tone="blue" /><MetricCard label="Collected" value={`৳${collectedAmount.toLocaleString()}`} icon={CheckCircle2} tone="violet" /></div>
      <div className="grid gap-5 xl:grid-cols-[1fr_300px]"><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row sm:items-center sm:justify-between sm:p-5"><div><h2 className="text-sm font-bold text-slate-950">Your order queue</h2><p className="mt-1 text-xs text-slate-500">Orders entered by the counter team</p></div><Link href="/manual-order-entry/orders" className="inline-flex items-center gap-1 text-xs font-semibold text-slate-600 hover:text-slate-950">View all <ArrowUpRight className="size-3.5" /></Link></div><div className="flex flex-col gap-2 border-b border-slate-100 p-4 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search order, customer or phone" className="h-10 w-full rounded-lg border border-slate-200 bg-slate-50 pl-9 pr-3 text-sm outline-none transition focus:border-emerald-500 focus:bg-white" /></label><select value={status} onChange={(event) => setStatus(event.target.value as "ALL" | Order["status"])} className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-slate-700 outline-none focus:border-emerald-500" aria-label="Filter orders by status"><option value="ALL">All statuses</option><option value="PENDING">Pending</option><option value="PROCESSING">Processing</option><option value="DELIVERED">Delivered</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-155 text-left text-sm"><thead className="bg-slate-50 text-xs font-semibold text-slate-500"><tr><th className="px-5 py-3">Order</th><th className="px-3 py-3">Customer</th><th className="px-3 py-3">Items</th><th className="px-3 py-3">Amount</th><th className="px-3 py-3">Status</th><th className="px-5 py-3 text-right">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{filteredOrders.map((order) => <tr key={order.id} className="transition-colors hover:bg-slate-50/70"><td className="px-5 py-4 font-mono text-xs font-semibold text-slate-900">{order.orderNumber}<span className="mt-1 block font-sans text-[11px] font-normal text-slate-400">{order.orderDate}</span></td><td className="px-3 py-4"><span className="block font-semibold text-slate-900">{order.customerName}</span><span className="text-xs text-slate-500">{order.customerPhone}</span></td><td className="px-3 py-4 text-slate-600">{order.items.reduce((count, item) => count + item.quantity, 0)} items</td><td className="px-3 py-4 font-semibold text-slate-900">৳{order.totalAmount.toLocaleString()}</td><td className="px-3 py-4"><span className={`inline-flex rounded-full border px-2 py-1 text-[11px] font-semibold ${statusStyles[order.status]}`}>{statusLabel(order.status)}</span></td><td className="px-5 py-4 text-right"><Link href="/manual-order-entry/orders" className="text-xs font-semibold text-emerald-700 hover:text-emerald-900">View</Link></td></tr>)}{filteredOrders.length === 0 && <tr><td colSpan={6} className="px-5 py-12 text-center text-sm text-slate-500">No manual orders match this search.</td></tr>}</tbody></table></div></section><aside className="space-y-5"><div className="rounded-2xl bg-slate-950 p-5 text-white shadow-sm"><div className="flex size-10 items-center justify-center rounded-xl bg-emerald-500/20 text-emerald-300"><PackageCheck className="size-5" /></div><h2 className="mt-5 text-lg font-bold">Ready for the next order?</h2><p className="mt-2 text-sm leading-6 text-slate-300">Search stock, add products, and capture delivery details in one quick flow.</p><Link href="/manual-order-entry/orders/new" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-emerald-300 hover:text-emerald-200">Open order entry <ArrowUpRight className="size-4" /></Link></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-bold text-slate-950">Quick links</h2><div className="mt-3 divide-y divide-slate-100"><QuickLink href="/manual-order-entry/products" label="Check live stock" detail="Find available products" /><QuickLink href="/manual-order-entry/customers" label="Find a customer" detail="Look up saved details" /><QuickLink href="/manual-order-entry/orders" label="Review all orders" detail="Track the full queue" /></div></div></aside></div>
    </div>
  );
}

function MetricCard({ label, value, icon: Icon, tone }: Readonly<{ label: string; value: number | string; icon: typeof ShoppingBag; tone: "emerald" | "amber" | "blue" | "violet" }>) { const tones = { emerald: "bg-emerald-50 text-emerald-700", amber: "bg-amber-50 text-amber-700", blue: "bg-blue-50 text-blue-700", violet: "bg-violet-50 text-violet-700" }; return <div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-slate-950">{value}</p></div><div className={`flex size-9 items-center justify-center rounded-xl ${tones[tone]}`}><Icon className="size-4" /></div></div></div>; }
function QuickLink({ href, label, detail }: Readonly<{ href: string; label: string; detail: string }>) { return <Link href={href} className="flex items-center justify-between gap-3 py-3 first:pt-0 last:pb-0"><span><span className="block text-sm font-semibold text-slate-800">{label}</span><span className="mt-0.5 block text-xs text-slate-500">{detail}</span></span><ArrowUpRight className="size-4 shrink-0 text-slate-400" /></Link>; }